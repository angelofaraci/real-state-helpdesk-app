"""Async arq worker package for stage-2 ticket classification (jobs +
`WorkerSettings`). See `app.workers.classification` for the job
implementations and `app.workers.settings` for the arq `WorkerSettings`
wiring."""
